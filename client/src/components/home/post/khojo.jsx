import { Box, Typography, styled } from "@mui/material";
import { addElipsis } from "../../../utils/common-utils";



const Text = styled(Typography)`
  font-size: 12px;
  color: #010101;
`;

const Heading = styled(Typography)`
  font-size: 18px;
  font-weight: 600;
`;

const Khojo = ({ post }) => {
  

  return (
    <>
      

    </>
  );
};
export default Khojo;
